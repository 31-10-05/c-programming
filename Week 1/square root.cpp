#include <stdio.h>
#include <math.h>
int main()
{
	float a,square_root;
	printf("Enter a number");
	scanf("%f",&a);
	square_root = sqrt(a);
	printf("Square root: %f", square_root);
	return 0;
}
